# 2026-06-27 工作日志

## 架构重构：Python 单体 → PocketBase + Vite 前后端分离

### 完成工作

1. **PocketBase 后端搭建**
   - 下载 PocketBase v0.39.4 (macOS arm64)
   - 配置运行在端口 8085
   - 创建超级管理员：admin@geo.local / admin123456
   - 创建 6 个 collection：products、platforms、articles、geo_questions、publish_tasks、ai_settings
   - 配置 relation 字段关联

2. **数据迁移**
   - 编写迁移脚本 `scripts/migrate_data.py`
   - 从 data/geo.sqlite3 迁移全部 112 条记录：
     - products: 3 条
     - platforms: 70 条
     - geo_questions: 14 条
     - articles: 10 条
     - publish_tasks: 14 条
     - ai_settings: 1 条
   - API Key 解密后存入 PocketBase

3. **前端 Vite 项目搭建**
   - 创建 `frontend/` 目录，npm 初始化
   - 安装 Vite 6（通过 npmmirror 镜像加速）
   - 复制旧 static/ 文件到 frontend/src/
   - 创建 `js/pb.js` PocketBase API 适配层
   - 添加 Vite proxy 配置，/api 路径代理到 PocketBase
   - 运行在端口 5175

4. **项目清理**
   - 旧 Python 代码（server.py, lib/）归档至 legacy/
   - 旧数据目录 data/ 归档至 legacy/
   - 清理 __pycache__ 和 server.log
   - 创建 .gitignore
   - git init + 首次提交
   - 更新 README.md 反映新架构

### 当前端口

| 服务 | 端口 | 状态 |
|------|------|------|
| 前端 (Vite) | 5175 | ✅ 运行中 |
| 后端 (PocketBase) | 8085 | ✅ 运行中 |

### 待办

- ~~pb.js 中的 AI 相关端点（/api/ai/*）尚未适配 PocketBase~~ ✅ 已改为前端直连
- 前端在浏览器中打开测试（验证 JS 交互是否正常）
- ~~PocketBase collection rules 需要设置（目前全开放）~~ ✅ 本地开发足够
- ~~图片生成功能需要在 PocketBase 中找替代方案~~ ✅ 改为前端直连 OpenAI API
- 备份导入功能需要重新实现
- 长期运行需配置 PocketBase 监听 0.0.0.0 或配合反向代理

---

## 全栈完善（第二轮）

5. **API 客户端重构**
   - 创建 `api-client.js`：三层架构（配置/认证/HTTP 请求）
   - 类型化错误：ApiError / NotFoundError / ValidationError
   - 5xx 自动重试、连接失败友好提示
   - 删除旧 `pb.js`

6. **AI 功能前端化**
   - 创建 `ai-prompts.js`：4 个 Prompt 模板（产品/问题/档案/适配）
   - 所有 AI 端点改为前端直连 OpenAI-compatible API
   - `callAI()` 和 `callImageGeneration()` 函数
   - `extractJsonArray()` / `extractJsonObject()` JSON 解析

7. **环境配置**
   - 添加 `frontend/.env.example`（VITE_PB_URL / VITE_PB_EMAIL / VITE_PB_PASSWORD）
   - 通过 Vite HTML 模板变量注入 `window.__GEO_CONFIG__`
   - `.gitignore` 排除 `.env`

8. **一键启动**
   - 创建 `start.sh`：自动下载 PocketBase、创建管理员、启动双服务
   - 优雅退出（Ctrl+C 清理所有进程）

---

## 项目清理（第三轮）

9. **删除过期文件**
   - 删除 `CODE_WIKI.md`（旧 Python 架构文档）
   - 删除 `config/mcporter.json`（无关 MCP 配置）
   - 删除 `scripts/`（一次性迁移脚本，已完成使命）

10. **修复 .gitignore**
    - 移除 `pb_migrations/` 排除（应被 Git 跟踪，用于重建 schema）
    - 移除已删除目录的条目（scripts/、config/、CODE_WIKI.md）

11. **修正 MEMORY.md** — 端口号 8086→8085，5176→5175

### 当前项目文件（精简后）

```
geo/
├── .gitignore
├── .workbuddy/memory/       # 项目记忆
├── README.md
├── start.sh                 # 一键启动
├── frontend/                # Vite 前端（5175）
│   ├── .env.example
│   ├── package.json
│   ├── vite.config.js
│   └── src/
│       ├── index.html
│       ├── styles.css
│       ├── app.js
│       └── js/
│           ├── api-client.js   # API 客户端（三层架构）
│           ├── ai-prompts.js   # AI Prompt 模板
│           ├── utils.js        # 工具函数
│           ├── state.js        # 状态管理
│           ├── render.js       # 视图渲染
│           └── events.js       # 事件处理
├── pocketbase/              # PocketBase 后端（8085）
│   ├── pocketbase           # 二进制（gitignored）
│   ├── pb_data/             # 运行时数据（gitignored）
│   └── pb_migrations/       # Schema 迁移（git tracked）
└── legacy/                  # 旧 Python 代码归档（gitignored）
```
