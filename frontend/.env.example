# GEO Studio — 前端环境变量
# 复制为 .env 并修改配置（.env 不提交到 Git）

# PocketBase 后端地址
# 留空 → 走同源：dev 由 vite proxy 转发到 127.0.0.1:8085，prod 由 PocketBase 同源提供
# 仅当后端与前端不同源（例如前端在 5175、后端在另一台机器）时才填写完整地址
VITE_PB_URL=

# 默认 AI API Key（可选）：填入后系统首次加载会自动启用 API 直连模式
# ⚠️ 注意：该值会在构建时被写入前端静态产物（window.__GEO_ENV__），任何能访问站点的人都能在源码里看到它。
#    仅适合「全员共享的受限 Key」场景；多用户场景请让每位用户在「AI 设置」里填自己的 Key，此处留空。
# 智谱 GLM 免费申请：https://open.bigmodel.cn/usercenter/apikeys
VITE_DEFAULT_AI_KEY=
